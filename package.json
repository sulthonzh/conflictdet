{
  "name": "conflictdet",
  "version": "1.0.0",
  "description": "Detect git merge conflict markers in source files. Zero dependencies.",
  "main": "src/index.js",
  "bin": { "conflictdet": "src/cli.js" },
  "scripts": {
    "test": "node --test test/*.js"
  },
  "keywords": ["git", "merge", "conflict", "marker", "detect", "ci"],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
